#include <iostream>
#include <array>

int main()
{
    std::string vypis, text2;
    int pole[5];
    int foo, bar, x, vypocet;

    std::string otazka, jmeno;
    int odpovedi[5];
    int i, j, skore, celkoveSkore;

    return 0;
}
